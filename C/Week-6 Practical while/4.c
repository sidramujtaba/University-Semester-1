#include<stdio.h>
int main() {
    int number = 1;
    while( number < 20){
        printf("\n%d", number); // if  given space intead of \n, itt will print will neat spaces, but in horizontal manner
        number++;
    }
    return 0;
}